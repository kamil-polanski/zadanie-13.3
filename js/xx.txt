
/*
paper.addEventListener(`click`, function() {
    if (rounds > 0) {
        playerMove = 1;
        pcMove = drawComputer();
        output.innerText = `Wybrałeś papier`;
        check();
    }
});

stone.addEventListener(`click`, function() {
    if (rounds > 0) {
        playerMove = 2;
        pcMove = drawComputer();
        output.innerText = `Wybrałeś kamień`;
        check();
    }
});

scissors.addEventListener(`click`, function() {
    if (rounds > 0) {
        playerMove = 3;
        pcMove = drawComputer();
        output.innerText = `Wybrałeś nożyczki`;
        check();
    }
});
*/



/*
var x;
var button = document.querySelectorAll(`.player-move`);
for (var i = 0; i < button.length; i++) {




    var x = button[i].addEventListener('click', function() {
        playerMove();

    });



}
*/
/*
var button = document.querySelectorAll(".player-move");
for (var ele of button) {


    var playerMoveName = ele.getAttribute("data-move");
    console.log(playerMoveName);
    ele.addEventListener("click", function() {
        console.log(ele);
    });
};
*/


/*
document.querySelectorAll(".player-move").forEach(function(button) {
    button.addEventListener("click", function() {
        console.log(button);
        var playerMoveName = button.getAttribute("data-move");
        console.log(playerMoveName);
    });
});
*/
/*
var playerMove = function(move) {
    var moveNumber;
    if (move == 'paper') {
        moveNumber = 1;
    } else if (move == 'rock') {
        moveNumber = 2;
    } else if (move == 'scissors') {
        moveNumber = 3;
    }

}


var btnChoice = document.querySelectorAll('.player-move');
for (var i = 0; i < btnChoice.length; i++) {
    var dataMove = btnChoice[i].getAttribute('data-move');
    console.log(dataMove)
    btnChoice[i].addEventListener('click', function() {
        playerMove(dataMove);
    });
};
console.log(playerMove);
*/
/*
var button = document.querySelectorAll(`.player-move`);
for (var i = 0; i < button.length; i++) {
    var x = button[i].addEventListener("click", bindClick(i));
    console.log(x);
}

function bindClick(i) {
    return function() {
        var y = button[i].getAttribute('data-move');
        console.log(y);
    };
}
*/